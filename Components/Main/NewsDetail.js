import React from 'react';
import {View}from 'react-native'
import { WebView } from 'react-native-webview';

function NewsDetailWebView ({route}) {

    return (
      <View style={{flex : 1}}>

      </View>
    );
}

export default NewsDetailWebView;